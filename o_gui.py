class Daily_digest_gui:

    def __init__(self,root):
        pass 

    """
        enable admin to :
        configure which content source to include in email
        -add recipients
        -remove re
        -schedule daily time 
        -configuire sender

    """
    if __name__== '__main___':
        root = TK()
        app=Daily_digest_gui(root)
        root.mainloop()
